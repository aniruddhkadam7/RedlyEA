/**
 * Feature flags for the Model Library and related toolbox migration.
 *
 * Self-contained — no coupling to unrelated modules.
 */

/**
 * When `false`, the legacy right-side toolbox panel in Studio is hidden.
 * The Model Library replaces it as the primary element catalog.
 *
 * Set to `true` to restore the old toolbox behavior.
 */
export const enableToolbox = false;
