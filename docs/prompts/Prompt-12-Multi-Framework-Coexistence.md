# Prompt 12 — Multi-Framework Coexistence

When multiple frameworks are enabled:

- Explorer shows the union of allowed types.
- Framework-specific elements are visually tagged.
- No automatic mapping or conversion is performed.
