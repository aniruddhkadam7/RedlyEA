# Prompt 15 — Explorer Filtering (Not Structural)

Explorer filters (by layer, framework, scope) must be visual filters only.

## Rules

- Filtering must never hide model existence.
- Filtering must never alter persistence.
